{
  "name": "outdated-check-action",
  "private": true,
  "type": "module",
  "devDependencies": {
    "@actions/core": "^1.10.1",
    "@google-cloud/monitoring": "^4.0.0",
    "@types/semver": "^7.5.5",
    "esbuild": "^0.19.6",
    "tsx": "^4.7.0",
    "typescript": "^5.2.2",
    "vitest": "^1.0.0"
  },
  "dependencies": {
    "semver": "^7.5.4"
  },
  "scripts": {
    "build": "npx esbuild --bundle ./src/run.ts --outdir=dist --platform=node --external:@actions/core --format=esm",
    "test": "vitest",
    "run-script": "tsx"
  }
}
